import 'package:flutter/material.dart';

class CarDeatisl extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: const Text('Car'),
      ),
    );
  }
}
